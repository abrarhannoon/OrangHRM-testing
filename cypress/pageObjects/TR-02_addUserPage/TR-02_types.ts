export const enum UserRole {
  SELECT = "-- Select --",
  ADMIN = "Admin",
  ESS = "ESS",
}

export const enum UserStatus {
  SELECT = "-- Select --",
  ENABLED = "Enabled",
  DISABLED = "Disabled",
}
